buildscript {

    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath BaseDependencies.gradle
        classpath BaseDependencies.kotlinGradlePlugin
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}